## 1. Конфигурация и каркас модуля

- [ ] 1.1 Добавить настройки Linear в `internal/config` и обновить `.env.example` переменными токена и доски.
- [ ] 1.2 Создать пакет `internal/linear` с публичными типами `Issue`, `Comment` и конструктором клиента.
- [ ] 1.3 Добавить локальную валидацию входных параметров для identifier задачи вида `ENG-123`, target state и тела комментария.

## 2. Реализация Linear API

- [ ] 2.1 Реализовать общий GraphQL transport на стандартном `net/http` с единым разбором ошибок Linear API.
- [ ] 2.2 Реализовать операцию чтения задачи только по Linear identifier с получением identifier, названия, описания, команды и текущего state.
- [ ] 2.3 Реализовать операцию получения списка задач с заданной Linear-доски с identifier, названием, описанием и текущим state.
- [ ] 2.4 Реализовать операцию смены state задачи по identifier и state ID или имени state в workflow команды.
- [ ] 2.5 Реализовать операции чтения комментариев обычным текстом и публикации нового комментария в задачу.

## 3. Тесты

- [ ] 3.1 Добавить table-driven тесты для конфигурации и локальной валидации Linear-модуля.
- [ ] 3.2 Добавить `httptest`-тесты для успешных и ошибочных сценариев списка задач, чтения задачи по identifier, смены state, чтения комментариев и публикации комментария.

## 4. CLI и документация модулей

- [ ] 4.1 Добавить минимальную CLI-команду для получения списка задач с заданной Linear-доски через модуль `internal/linear`.
- [ ] 4.2 Создать индекс `docs/modules/README.md` со списком модулей и кратким описанием каждого.
- [ ] 4.3 Добавить `docs/modules/linear.md` с обязательными разделами по шаблону модульной документации.
- [ ] 4.4 Зафиксировать правило обновления документации модуля при изменении публичного API, конфигурации или внешних зависимостей.

## 5. Проверка change

- [ ] 5.1 Прогнать `go fmt ./...`.
- [ ] 5.2 Прогнать `go test ./...`.
