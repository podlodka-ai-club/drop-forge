## 1. Конфиг и контракт TaskManager

- [ ] 1.1 Расширить `internal/config` настройками Linear, обязательным project filter и маппингом управляемых state'ов `ready to propose`, `ready to code` и `ready to archive`.
- [ ] 1.2 Обновить `.env.example` всеми новыми `LINEAR_*` ключами без значений и добавить unit-тесты на загрузку, валидацию, приоритет process env и ошибку при отсутствии project filter/state mapping.
- [ ] 1.3 Описать минимальные внутренние типы `TaskManager`: модель задачи, включающую описание, текущий state и комментарии, а также операции `GetTasks`, `MoveTask`, `AddComment` и `AddPR`.

## 2. Чтение задач из Linear

- [ ] 2.1 Добавить пакет `internal/taskmanager` и/или внутренний Linear adapter для загрузки задач только из настроенного проекта и управляемых state'ов.
- [ ] 2.2 Реализовать возврат данных задачи в едином payload для `CoreOrch`: идентификатор, key, описание, state и комментарии.
- [ ] 2.3 Покрыть чтение задач тестами на project-scoped фильтрацию, фильтрацию по state, стабильный payload без description/comments и возврат обновленных комментариев после повторного получения задачи.
- [ ] 2.4 Добавить отдельный тест сценария повторного чтения задачи: после reject и новых human comments следующий вызов `GetTasks` через `CoreOrch -> TaskManager` возвращает ту же задачу с обновленным feedback.

## 3. Запись обратно в Linear

- [ ] 3.1 Реализовать в `TaskManager` операцию перевода задачи в новый state.
- [ ] 3.2 Реализовать в `TaskManager` операцию публикации комментария в задаче.
- [ ] 3.3 Реализовать в `TaskManager` операцию привязки PR к задаче.
- [ ] 3.4 Покрыть write-операции тестами на корректное формирование запросов, привязку данных к правильной задаче и обработку ошибок Linear API.
- [ ] 3.5 Добавить негативные тесты на невалидный PR URL, частично заполненный ответ Linear и сетевые/transport ошибки клиента.

## 4. Wiring и документация

- [ ] 4.1 Добавить structured logging для чтения и записи задач через `taskmanager` и `linear` модули.
- [ ] 4.2 Зафиксировать в коде и документации, что `TaskManager` является Linear-facing слоем для будущего `CoreOrch`, а не orchestration-loop.
- [ ] 4.3 Обновить README и связанную документацию описанием project-scoped TaskManager и передачи комментариев вместе с описанием задачи.
- [ ] 4.4 Добавить unit-тесты на structured logging ошибок и операций чтения/записи с контекстом project/state/task.
- [ ] 4.5 Прогнать `go fmt ./...` и `go test ./...`.
