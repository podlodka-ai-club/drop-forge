## 1. Введение типа `ProposalInput` и обновление раннера

- [ ] 1.1 В `internal/proposalrunner/runner.go` определить публичный тип `ProposalInput` с полями `Title`, `Identifier`, `AgentPrompt` (все `string`).
- [ ] 1.2 Изменить сигнатуру `func (runner *Runner) Run(ctx context.Context, input ProposalInput) (string, error)`. Добавить валидацию: `Title` и `AgentPrompt` после `TrimSpace` непустые, иначе вернуть осмысленные ошибки до создания temp-директории.
- [ ] 1.3 Внутри `Run` нормализовать `Title` (заменить `\n`/`\r` на пробел, `TrimSpace`) и сформировать `displayName`: `"<Identifier>: <Title>"` если `Identifier` непустой, иначе `Title`.
- [ ] 1.4 `prTitle = BuildPRTitle(prefix, displayName)`, `branchName = BuildBranchName(prefix, displayName, runner.now())`. Передавать `input.AgentPrompt` (а не сериализованный input) в `agentExecutor.Run` через `AgentExecutionInput.TaskDescription`.
- [ ] 1.5 Удалить `firstLine` (или преобразовать во внутренний хелпер только для нормализации `Title`, без вызова из `BuildPRTitle`/`BuildBranchName`). Убедиться, что `BuildPRTitle` теперь принимает уже готовый `displayName` и не извлекает «первую строку».
- [ ] 1.6 Обновить `BuildPRBody` так, чтобы тело PR строилось из `AgentPrompt` (не из displayName), сохранив текущий формат «Generated by orchv3 proposal runner.\n\nTask:\n…».

## 2. Обновление `coreorch` и интерфейса `ProposalRunner`

- [ ] 2.1 В `internal/coreorch/orchestrator.go` поменять интерфейс `ProposalRunner` на `Run(ctx context.Context, input proposalrunner.ProposalInput) (string, error)`. Импортировать `proposalrunner` (если ещё не импортируется).
- [ ] 2.2 Переписать `BuildProposalInput(task taskmanager.Task) proposalrunner.ProposalInput`: заполнить `Title` (с fallback `"Untitled task"` при пустом `task.Title`), `Identifier` (`TrimSpace`), `AgentPrompt` — текущий многострочный блок (Linear task / ID / Identifier / Title / Description / Comments) без изменения формата.
- [ ] 2.3 В `processTask` передавать новый `ProposalInput` в `orch.ProposalRunner.Run`. Убедиться, что логирование `taskRef` и обработка ошибок остаются прежними.

## 3. Обновление CLI

- [ ] 3.1 В `cmd/orchv3/main.go` поменять `singleProposalRunner` интерфейс на `Run(ctx context.Context, input proposalrunner.ProposalInput) (string, error)`.
- [ ] 3.2 В direct-режиме строить `proposalrunner.ProposalInput{Title: text, Identifier: "", AgentPrompt: text}` из результата `readTaskDescription` и передавать в `runner.Run`.
- [ ] 3.3 Убедиться, что `defaultDeps().newProposalRunner` по-прежнему возвращает `*proposalrunner.Runner`, удовлетворяющий новому интерфейсу.

## 4. Обновление тестов под новый контракт

- [ ] 4.1 `internal/proposalrunner/runner_test.go`: переписать тесты `Run`/`BuildPRTitle`/`BuildBranchName` под `ProposalInput`. Добавить case'ы: пустой `Title` → ошибка, пустой `AgentPrompt` → ошибка, `Title` с `\n` → нормализация, `Identifier` пустой → fallback на `Title`, длинный `Title` → truncation до 72 рун.
- [ ] 4.2 `internal/coreorch/orchestrator_test.go`: обновить `recordingProposalRunner` под новую сигнатуру, заменить хранение `inputs []string` на `inputs []proposalrunner.ProposalInput`. Поправить ассерты в существующих тестах.
- [ ] 4.3 `cmd/orchv3/main_test.go`: обновить `fakeSingleProposalRunner` под новую сигнатуру; в тестах direct-режима проверить, что `Title` и `AgentPrompt` равны введённой строке, `Identifier` пуст.

## 5. Интеграционный тест на стык orchestrator → runner

- [ ] 5.1 Добавить в `internal/coreorch/orchestrator_test.go` (или в новый `integration_test.go` пакета `coreorch`) тест `TestBuildProposalInputProducesPRTitleFromTaskTitle`:
  - построить `taskmanager.Task` с `Identifier="ZIM-42"`, `Title="Add export feature"`, непустыми `Description` и `Comments`;
  - вызвать `coreorch.BuildProposalInput(task)` и затем `proposalrunner.BuildPRTitle(prefix, displayName(input))` (или эквивалентный публичный helper, см. задачу 1.5);
  - утверждать, что результат содержит `"ZIM-42: Add export feature"` и не содержит `"Linear task:"`.
- [ ] 5.2 Добавить кейс с пустым `Identifier`: ожидать `prTitle` без префикса `"<Identifier>: "`.
- [ ] 5.3 Добавить кейс с пустым `Title`: ожидать, что fallback из `BuildProposalInput` (`"Untitled task"` или эквивалент) попадает в `prTitle` и не вызывает ошибку валидации в раннере.

## 6. Документация и проверки

- [ ] 6.1 Обновить `docs/proposal-runner.md` — описать новый контракт `ProposalInput` и правило формирования `prTitle`/branch/commit.
- [ ] 6.2 Обновить `architecture.md`, если в нём упоминается сигнатура `ProposalRunner.Run(ctx, string)`.
- [ ] 6.3 Запустить `go fmt ./...` и `go test ./...`; убедиться, что все тесты зелёные.
- [ ] 6.4 Прогнать `openspec validate pass-task-metadata-to-proposal-runner --strict` и убедиться, что валидация change'а проходит.
