## 1. Сбор и сверка контекста

- [x] 1.1 Проверить фактические сценарии запуска в `cmd/orchv3/main.go` и не описывать в README неподдерживаемые режимы.
- [x] 1.2 Сверить зависимости и переменные окружения по `go.mod`, `internal/config/config.go`, `.env.example` и `docs/proposal-runner.md`.

## 2. Подготовка README

- [x] 2.1 Создать корневой `README.md` с описанием назначения проекта и текущего proposal-runner workflow.
- [x] 2.2 Добавить разделы с зависимостями, настройкой `.env`, примерами запуска через аргументы CLI и `stdin`, а также пояснение про `stdout` и `stderr`.
- [x] 2.3 Добавить в README ссылки на `docs/proposal-runner.md`, ключевые директории проекта и базовые команды проверки `go fmt ./...` и `go test ./...`.

## 3. Проверка согласованности

- [x] 3.1 Проверить, что README согласован с существующим кодом и не дублирует детали, которые уже лучше раскрыты в `docs/proposal-runner.md`.
- [x] 3.2 При необходимости уточнить формулировки в смежной документации, если после добавления README остаются явные расхождения.
