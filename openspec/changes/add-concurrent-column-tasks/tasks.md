## 1. Подготовка CoreOrch

- [ ] 1.1 Выделить в `internal/coreorch` grouping задач по стадиям: proposal, apply, archive, сохраняя порядок задач внутри каждой стадии.
- [ ] 1.2 Добавить stage runner helper, который обрабатывает список задач одной стадии последовательно существующими `process*Task` методами.
- [ ] 1.3 Сохранить существующие no-ready-tasks логи для пустых стадий.

## 2. Конкурентное выполнение стадий

- [ ] 2.1 Запускать отдельную goroutine для каждой непустой стадии в рамках `RunProposalsOnce`.
- [ ] 2.2 Дождаться завершения всех запущенных stage goroutine перед возвратом из `RunProposalsOnce`.
- [ ] 2.3 Собрать ошибки стадий через channel или аналогичный простой механизм и вернуть агрегированную ошибку с названием стадии и исходным контекстом.
- [ ] 2.4 Не отменять общий `ctx` при первой ошибке стадии; полагаться только на внешнюю отмену контекста.
- [ ] 2.5 Убедиться, что concurrent log writes остаются валидными structured events; при необходимости добавить минимальную синхронизацию записи.

## 3. Тесты

- [ ] 3.1 Обновить тесты, которые ожидают полностью последовательную обработку proposal/apply/archive в одном pass.
- [ ] 3.2 Добавить unit test, доказывающий, что задачи из разных ready-колонок стартуют без ожидания завершения друг друга.
- [ ] 3.3 Добавить unit test, доказывающий, что несколько задач одной стадии продолжают выполняться последовательно в порядке `TaskManager`.
- [ ] 3.4 Добавить unit test, доказывающий, что ошибка одной стадии не останавливает уже запущенную другую стадию.
- [ ] 3.5 Добавить unit test для агрегированной ошибки при нескольких падающих стадиях.

## 4. Документация и валидация

- [ ] 4.1 Обновить `architecture.md`: описать, что `CoreOrch` получает задачи одним pass и запускает stage route параллельно по колонкам.
- [ ] 4.2 Запустить `openspec validate add-concurrent-column-tasks --strict` и исправить найденные проблемы.
- [ ] 4.3 Запустить `go fmt ./...`.
- [ ] 4.4 Запустить `go test ./...`.
