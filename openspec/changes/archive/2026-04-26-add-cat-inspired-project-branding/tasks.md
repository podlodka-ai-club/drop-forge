## 1. Brand Assets

- [x] 1.1 Create `docs/assets/logo.svg` with a simple cat-inspired `Purrch` logo that renders without external assets or build steps.
- [x] 1.2 Ensure the logo remains readable at README scale and includes accessible text/metadata where practical for SVG.

## 2. Documentation Updates

- [x] 2.1 Update `README.md` to use `Purrch` as the public project name and show the logo near the top of the document.
- [x] 2.2 Keep `orchv3` visible in README as the current technical CLI/module name and preserve existing run commands.
- [x] 2.3 Add concise branding guidance in `docs/branding.md` that explains when to use `Purrch` and when to use `orchv3`.
- [x] 2.4 Verify README links to the logo and branding guide resolve correctly from the repository root.

## 3. Validation

- [x] 3.1 Run `go fmt ./...`.
- [x] 3.2 Run `go test ./...`.
- [x] 3.3 Run the relevant OpenSpec status/validation command for `add-cat-inspired-project-branding` and confirm the change remains apply-ready.
